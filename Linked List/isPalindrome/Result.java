package isPalindrome;

import helper.linkedlist.LinkedListNode;

public class Result {
	public boolean mResult;
	public LinkedListNode mNode;
	
	public Result(LinkedListNode node, boolean result) {
		mNode = node;
		mResult = result;
	}
}
